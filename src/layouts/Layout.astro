<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>Panel</title>
    <link rel="preconnect" href="https://api.pearktrue.cn" />
    <link rel="preconnect" href="https://t.alcy.cc" />
    <link rel="stylesheet" href="https://cdn.bootcdn.net/ajax/libs/font-awesome/6.4.2/css/all.min.css" />
  </head>
  <body class="relative min-h-screen antialiased bg-[#1a1a1a]">
    <div class="fixed inset-0 bg-black/35 pointer-events-none z-[-1]"></div>
    <slot />
  </body>
</html>

<style is:global>
  body::before {
    content: "";
    position: fixed;
    inset: 0;
    background: var(--bg-url) center/cover no-repeat fixed;
    opacity: 0;
    transition: opacity 1s ease-in-out;
    z-index: -2;
  }
  body.bg-loaded::before { opacity: 1; }
  ::-webkit-scrollbar { display: none; }
  html { scrollbar-width: none; -ms-overflow-style: none; }
</style>