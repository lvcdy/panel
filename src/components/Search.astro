---
import { SEARCH_ENGINES } from "../data/links";
---

<search
    class="mt-10 sm:mt-12 w-full max-w-[650px] px-4 sm:px-6 relative animate-fade-in-up z-20"
    style="animation-delay: 200ms"
>
    <div
        class="search-container glass flex items-center h-16 rounded-2xl px-2 shadow-2xl"
        role="search"
    >
        <button
            type="button"
            id="engineBtn"
            class="w-12 h-12 flex items-center justify-center rounded-xl cursor-pointer hover:bg-white/10 transition-all"
            aria-label="选择搜索引擎"
            aria-haspopup="listbox"
            aria-expanded="false"
        >
            <i
                id="currentIcon"
                class="fab fa-microsoft text-xl transition-all"
                style="color: #00a4ef"
                aria-hidden="true"></i>
        </button>
        <input
            type="search"
            id="searchInput"
            placeholder="正在思考..."
            class="bg-transparent flex-grow h-full px-4 outline-none placeholder-white/40 font-medium"
            autocomplete="off"
            aria-label="搜索"
        />
        <button
            type="button"
            id="searchBtn"
            class="mr-4 p-2 opacity-40 cursor-pointer hover:opacity-70 transition-opacity"
            aria-label="执行搜索"
        >
            <i class="fas fa-search" aria-hidden="true"></i>
        </button>
    </div>
    <div
        id="searchTip"
        class="mt-2 text-center text-red-400 text-sm opacity-0 transition-opacity duration-300"
        role="alert"
        aria-live="polite"
    >
        请输入搜索内容
    </div>
    <div
        id="engineMenu"
        class="engine-menu absolute top-full left-4 sm:left-0 w-48 rounded-2xl hidden flex-col z-50 overflow-hidden mt-3 p-1.5"
        role="listbox"
        aria-label="搜索引擎列表"
    >
        {
            SEARCH_ENGINES.map((e, i) => (
                <div
                    class:list={[
                        "engine-item group relative flex items-center gap-3 px-3.5 py-2.5 rounded-xl cursor-pointer text-sm font-medium text-white/80",
                        i === 0 && "active",
                    ]}
                    data-url={e.url}
                    data-icon={e.icon}
                    data-color={e.color}
                    role="option"
                    tabindex="0"
                    aria-selected={i === 0 ? "true" : "false"}
                >
                    <span
                        class="engine-icon-wrap w-8 h-8 flex items-center justify-center rounded-lg transition-colors duration-200"
                        style={`background: ${e.color}15`}
                    >
                        {e.id === "google" ? (
                            <svg
                                class="w-4 h-4"
                                viewBox="0 0 24 24"
                                aria-hidden="true"
                            >
                                <path
                                    d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92a5.06 5.06 0 0 1-2.2 3.32v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.1z"
                                    fill="#4285F4"
                                />
                                <path
                                    d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"
                                    fill="#34A853"
                                />
                                <path
                                    d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"
                                    fill="#FBBC05"
                                />
                                <path
                                    d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"
                                    fill="#EA4335"
                                />
                            </svg>
                        ) : (
                            <i
                                class={`${e.icon} text-sm`}
                                style={`color: ${e.color}`}
                                aria-hidden="true"
                            />
                        )}
                    </span>
                    <span class="flex-1 truncate">{e.name}</span>
                    <i
                        class="fas fa-check text-[10px] opacity-0 transition-opacity duration-150 engine-check"
                        aria-hidden="true"
                    />
                </div>
            ))
        }
    </div>
</search>
